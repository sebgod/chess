{
  "name": "chess-firebase-rules",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Security rules for the cloud correspondence courier, and the only tests for logic that does not live in the C# tree.",
  "scripts": {
    "test": "firebase emulators:exec --project demo-chess \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
